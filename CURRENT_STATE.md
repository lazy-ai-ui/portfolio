# Текущее состояние

Обновлять по ходу работы. Дата последней правки: **2 августа 2026**,
ветка `main`, 29 коммитов.

---

## Коротко

**Кейс RackTables — готов целиком, заглушек не осталось.**
**Главная — каркас готов, но пустая по контенту:** нет фото, тексты «Обо мне»
заглушечные, второй кейс не собран.

Ближайшая осмысленная задача — довести главную до состояния, в котором её не
стыдно показать: фото, тексты о себе, ссылки.

---

## Что сделано

### Инфраструктура

- Проект разобран из двух монолитных HTML в нормальную структуру:
  `css/` (5 файлов), `js/` (9 модулей), `assets/`, `content/`.
- Гит подключён, Netlify деплоит сам с пуша в `main`.
- Шрифты локализованы, Google Fonts убран.
- Все картинки в webp: 12 файлов, суммарно ~1.2 МБ вместо ~6 МБ исходников.
  Исходники лежат в `assets/img/_src/`, в гит не попадают.

### Кейс `case-racktables.html` — 8 разделов, все заполнены

| Раздел | Что внутри |
|---|---|
| Обзор | Два скриншота (светлая/тёмная), три карточки фактов, сноска о недоказуемости цифр |
| Процесс | Три фазы |
| Интервью | Таблица вопросов, сравнение двух респондентов, оговорка о размере выборки |
| Аналоги | Полная таблица 4 систем × 5 критериев + вывод |
| Проблемы | Четыре инсайта |
| JTBD | Четыре карточки: ситуация → требование → решение |
| Решение | Слайдер «было/стало», аннотированный дашборд, два флоу по шагам |
| Итог | Видно на макетах / что покажут тесты / рефлексия |

### Интерактив

- **Хиро:** светлый, без тёмной плашки — частицы тёмные, собираются в один из
  трёх макетов, синхронно скрэмблится строка с фразой этого макета. Плотность
  облака падает от центра к краю. Над фактами стоит фраза-якорь (`h1`).
- **Итог кейса:** светлый, как и вся страница. Три колонки разведены по
  смыслу — «Видно на макетах», гипотезы в карточке с плашкой, «Рефлексия»
  нумерованным трекером.
- **Аннотации на дашборде:** 4 пульсирующие точки, выноска собирается из
  частиц (десктоп) или выезжает сбоку (мобилка), закрытие по клику вне и Esc,
  увеличенный просмотр в модалке, подводка к выноске минимальной прокруткой.
- **Слайдер «было/стало»:** мышь, тач, стрелки; подписи прячутся, когда их
  сторона ужата.
- **Два флоу** на общем компоненте: лента шагов + закреплённый экран. На
  десктопе экран приколот по центру окна на всю длину ленты, шаг и скриншот
  меняются сами при прокрутке, каждый шаг держится 52vh. На мобилке экран
  липнет под навбаром — так и было, не трогали. Клик и стрелки оставлены как
  способ вернуться к нужному шагу.
- **Навигация:** стеклянная, активный раздел — переезжающая пилюля.
- **Навбар:** имени в пилюле нет — оно и так стоит в заголовке первого экрана.
  Пилюля навбара и пилюля статуса одной высоты (37.75px, на мобиле 34.5px),
  размер собран переменными `--pill-*`. :hover-стили обёрнуты в
  `@media (hover: hover)` — на тач-устройствах ховер не зависает после тапа.
- **Появление блоков при скролле** — один раз за загрузку.
- **Кнопка «Наверх»** есть только на страницах кейсов, появляется когда
  пользователь долистывает до раздела «Итог».

### Тексты

Кейс вычитан по `content/UX Copywriting Rules.md`. Видимый текст ужат с 13 799
до 12 296 знаков (11%). Вопросы интервью, ответы респондентов и формулировки
JTBD не тронуты намеренно.

---

## Что осталось

### 1. Главная — контент (это блокирует всё остальное)

- [ ] **Фото.** Два места: аватар 64×64 в хиро и крупное фото в «Обо мне».
      Сейчас `<img src="">` с `onerror`, который их прячет.
- [ ] **Тексты «Обо мне».** Три абзаца — сейчас инструкция самому себе о том,
      что там должно быть, а не текст. Писать по правилам из
      `UX Copywriting Rules.md`.
- [ ] **Ссылка на Behance** — стоит `href="#"`. Либо адрес, либо убрать.
- [ ] Обложка второго проекта — плашка «не собрано» со статичной россыпью,
      это осознанная заглушка до второго кейса.

### 2. Второй кейс

Каркас переиспользуется целиком: все компоненты кейса (таблицы, флоу,
аннотации, слайдер) уже общие и работают с любым содержимым.

### 3. Возможная доработка текста

Сокращение вышло 11% при ориентире инструкции 30–50%. Осталось так потому, что
почти 40% текста — либо дословные слова респондентов, либо таблица конкурентов,
которую ментор просил показать целиком. Если решим добивать до 30%, придётся
выбрать, чем платим: ужать ячейки конкурентов до одной строки на критерий
(потеряется конкретика вроде «после 3.4 деградировал») или убрать колонку
«Alerts». Решение за Фирдавсом.

---

## Известные ограничения

Не баги, но знать надо.

- **Тач-ветка хиро не проверена вживую.** Панель предпросмотра сообщает
  `hover: none = false` на любой ширине, поэтому тап по частицам, смена макета
  по повторному тапу и автовозврат через 4.5 с проверены только по коду.
- **Плавность анимаций не проверена.** Сборка выносок, переезд пилюли,
  появление блоков, `sticky` у экранов флоу, угасание частиц — вся логика и
  геометрия замерены, но сами кадры в этой среде не рисуются. Смотреть глазами.
- **Модалка на мобилке почти не увеличивает макет.** Пропорция 1.4:1 упирается
  в ширину экрана: 327px против 335px обычных. Лечится только панорамированием
  (показывать макет шире экрана и таскать пальцем) — не делали.
- **Слайдер «было/стало»: разные пропорции.** Старый экран 1.95, новый 1.41.
  Старый вписан целиком на нейтральной подложке, поля сверху и снизу примерно
  по 14% высоты. Обрезать нельзя — уедет третий ряд плиток, и цифра «12
  разделов» перестанет читаться.
- **Темп автопрокрутки флоу не пробовали вживую.** Шаг держится 420–443px
  прокрутки при окне 900px — посчитано по позициям, не по ощущению. Разброс
  от длины текста шага, так и задумано. Крутится одним числом: `padding-bottom`
  у `.flow__step` в десктопном блоке `case.css`. Именно `padding`, а не
  `min-height`: кнопка центрирует содержимое по вертикали, когда она выше
  него, и текст отрывается от своей засечки на пунктире. Если менять — заодно
  пересчитать хвост `padding-bottom` у `.flow__rail`, он держит картинку
  приколотой, пока читают последний шаг (сейчас ~330px запаса).
- **Нумерация в выносках на скриншотах флоу сбита** — на экранах IP-конфликта
  третий и четвёртый помечены одинаково «3/4». В ленте шагов не видно, но при
  пересъёмке стоит поправить.

## Открытых багов нет

Всё, что находили, починено: схлопывание блока аннотаций, растяжение холста на
весь экран, протекание десктопных ширин в мобильные таблицы, невидимые блоки
из-за веса селекторов, обрезка частиц по краям, вылет тёмной обложки за рамку.
Подробности — в истории коммитов, там у каждого описана причина.

---

## Откат

Перед вычиткой текста поставлена метка:

```bash
git reset --hard before-copy-edit
```

Она на коммите `01e7230` — состояние до правок текста, всё остальное в силе.

---

## С чего начать в новом чате

1. Прочитать `PROJECT_CONTEXT.md` целиком — там визуальный язык, правила текста
   и грабли этого кода.
2. Прочитать `content/UX Copywriting Rules.md`, если задача про текст.
3. Спросить у Фирдавса фото и тексты «Обо мне» — без них главная не двигается.
