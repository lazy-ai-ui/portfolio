.DS_Store
Thumbs.db
node_modules/
*.log
.claude/settings.local.json
.claude/scheduled_tasks.lock
# исходники картинок до пережатия в webp — в деплой не нужны
assets/img/_src/
# рабочие материалы кейса Nearby: экраны, референсы, скрины отзывов (~16 МБ).
# В деплой не идут, в гите не нужны — по той же причине, что и _src.
# Тексты и контекстные .md из этой папки отслеживаются.
case nearby/**/*.png
case nearby/**/*.jpg
case nearby/**/*.webp
case nearby/**/desktop.ini
.zcode/
# локальные бекапы перед крупными правками — в деплой и в историю не нужны
.backup/
